<%= form_for [@user, Item.new] do |f| %>
<div>
<div class="form-group">
  <%= f.label :new_item %>
  <%= f.text_field :name, placeholder: "Enter a new item!", autofocus: true %>
</div>

<div class="form-group">
  <%= f.submit "Submit Item", class: 'btn btn-success' %>
</div>
</div>
<% end %>
